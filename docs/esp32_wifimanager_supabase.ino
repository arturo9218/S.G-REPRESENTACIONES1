/*
 * Este sketch se reemplazó por el firmware combistato + NTC + Supabase:
 *
 *   docs/esp32_combistato_supabase.ino
 *
 * Motivo: en ESP32 el producto pasa a combistato con sondas NTC 10k (ADC)
 * y relés (sin pinza SCT). El archivo `esp8266_wifimanager_supabase.ino`
 * sigue siendo la referencia para monitor simple con DS18B20 + SCT en ESP8266.
 */
